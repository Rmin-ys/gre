🛠 نحوه نصب و اجرا (Installation)
بسته به نیاز خود، یکی از دو اسکریپت زیر را انتخاب کرده و در هر دو سرور (ایران و خارج) اجرا کنید.

نکته: دستورات زیر به صورت هوشمند اسکریپت را دانلود و مستقیماً اجرا می‌کنند.

۱. مدل HAProxy (پیشنهادی برای ترافیک انبوه و TCP)
این نسخه برای کسانی است که از پنل‌های V2Ray استفاده می‌کنند و نیاز به پایداری بالا در ترافیک TCP دارند.

Bash
bash <(curl -Ls https://raw.githubusercontent.com/YOUR_USERNAME/YOUR_REPO/main/gre.sh)
۲. مدل Socat (مناسب برای گیمینگ و UDP)
اگر نیاز به انتقال ترافیک UDP (مانند بازی‌های آنلاین) دارید، از این نسخه استفاده کنید.

Bash
bash <(curl -Ls https://raw.githubusercontent.com/YOUR_USERNAME/YOUR_REPO/main/socat.sh)
